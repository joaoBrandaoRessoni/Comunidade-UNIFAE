<%- include('partials/header') %>

<body>
    <div class="container px-3 pb-3 px-md-0 pb-md-0 d-flex align-items-center justify-content-center" id="grupo_inicial">
        <div class="row mt-3 rounded shadow">
            <div class="col-md-7 text-center d-flex flex-column justify-content-center px-5 bg-unifae">
                <h3>Bem vindo à comunidade Unifae</h3>
                <p>Aqui você pode enviar suas perguntas e deixar que a comunidade da UNIFAE composta por seus alunos responda suas perguntas</p>
            </div>
            <div class="col-md-5 text-center py-3 bg-white">
                <h3>Faça seu login</h3>
                <form method="post" action="/login" class="d-flex flex-column align-items-center py-3 gap-3">
                    <label>
                        <span>Usuario</span>
                        <% if(erro){ %>
                            <input type="text" name="user" id="" class="form-control is-invalid" required>
                        <% }else { %>
                            <input type="text" name="user" id="" class="form-control" required>
                        <% } %>
                    </label>
                    <label>
                        <span>Senha</span>
                        <% if(erro){ %>
                            <input type="password" name="senha" id="" class="form-control is-invalid" required>
                        <% }else { %>
                            <input type="password" name="senha" id="" class="form-control" required>
                        <% } %>
                    </label>
                    <button type="submit" class="btn btn-unifae">Logar</button>
                </form>
            </div>
        </div>
    </div>
</body>

<%- include('partials/footer') %>